# 実行可能な作図例

`scripts/qcad-draw.sh` でそのまま実行できるスニペット。

```bash
scripts/qcad-draw.sh references/examples/motor-mount.js --png
scripts/qcad-draw.sh references/examples/l-bracket.js --png
```

- `motor-mount.js` — 4隅 R8 フィレット、PCD70 ボルト穴配列、複数レイヤ構成の総合例
- `l-bracket.js` — 凸コーナーと凹（再入）コーナーのフィレットを併用する例
