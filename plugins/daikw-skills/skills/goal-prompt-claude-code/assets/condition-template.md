<!--
Claude Code /goal condition テンプレ（L1 通常モード用）。
公式 /goal <condition> にそのまま貼る。
プレースホルダ {{...}} を埋めて生成する。
4000 char 上限厳守。3500 で警告、超過なら L3 切替を提案。
連携モード (autoresearch handoff) 用は assets/autoresearch-iteration-template.md。
-->

{{ONE_LINE_MEASURABLE_END_STATE}}

Prove completion by:
{{#each PROOF_STEPS}}
- {{this.step_number}}. {{this.action}} — surface the result in the transcript so the evaluator can confirm
{{/each}}

{{#if METRIC_BOUND_CLAUSE}}
Metric threshold (also stops the goal):
- {{METRIC_BOUND_CLAUSE}}
{{/if}}

Out of scope:
- do not modify {{OUT_OF_SCOPE_PATHS}}
- preserve {{INVARIANTS}}

Bounding:
- stop after {{TURN_LIMIT}} turns
- stop if the same error repeats 3 times
- on stop, summarize remaining work in {{REPORT_FILE_OR_TRANSCRIPT}}

<!--
Generated by: goal-prompt-claude-code (L1, normal) from {{SEED_SOURCE}} at {{DATE}}
char count: {{CHAR_COUNT}}/4000

If char count > 3500: warning, consider L3 (agent hook) instead.
If char count > 4000: L3 切替必須 — evaluator が tool 使えるので proof method を condition から省略できる。
-->
