<!--
Claude Code /goal autoresearch 連携モード用テンプレート (L1)。
autoresearch の 1 iteration 内のサブゴール記述に特化。
4000 char 制約下で薄く保つ。1000 char 以下推奨。
プレースホルダ {{...}} を埋めて生成する。
-->

Sub-goal of autoresearch iteration:
{{ONE_LINE_ACTION_FOR_THIS_ITERATION}}

Local check (this iteration only):
{{LOCAL_CHECK_COMMAND}} → {{EXPECTED_OUTPUT}}

Constraints:
- This is ONE iteration. Do NOT converge global metric here.
- If local check fails, report findings to `ITERATION_RESULT.md` and stop.
- Do not modify {{OUT_OF_ITERATION_SCOPE}}.
- Stop after {{N_TURNS}} turns.

<!--
Generated by: goal-prompt-claude-code (autoresearch-handoff, L1) from {{SEED_SOURCE}} at {{DATE}}
char count: {{CHAR_COUNT}}/4000 (target ≤ 1000 for iteration sub-goals)

Note: This is a sub-goal for one iteration of an outer autoresearch loop.
To set up the full loop:

  /autoresearch:plan
  Goal: {{ORIGINAL_USER_GOAL}}

autoresearch will define Scope/Metric/Direction/Verify, then call this
sub-goal (or similar) once per iteration.
-->
